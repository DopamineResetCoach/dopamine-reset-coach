app_identifier("com.dopaminereset.coach")
apple_id(ENV["APPLE_ID"] || "niek_damen@live.nl")

# Your Apple Team ID — find at https://developer.apple.com/account → Membership
# (Looks like a 10-char string e.g. "ABCD123456")
# team_id("XXXXXXXXXX")

# App Store Connect API Key — set via env or fastlane/api_key.json
# Generate one at https://appstoreconnect.apple.com/access/api
# Store the .p8 file inside this fastlane/ folder and reference it via env vars
# (set ASC_KEY_ID, ASC_ISSUER_ID, ASC_KEY_FILEPATH) — Fastfile reads them.
